#include <iostream>

using namespace std;

vector<string> generateParenthesis(int n);
int main() {
  return 0;
}

vector<string> generateParenthesis(int n) {
  vector<string> res;
  for (int i = 0; i < n; i++) {
                            
  }   
  return res;
}
